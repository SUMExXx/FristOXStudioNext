type QNAElementProps = {
    question: string;
    answer: string;
}

enum Types3D {
    shirt = 1,
    pants = 2,
}

type PaymentProps = {
    name: string;
    email: string;
    plan: string;
    amount: string;
    orderID: string;
}